
const integers = [4, 2, 6, 8, 1];

if (integers[0] === 1 || integers[integers.length - 1] === 1) {
    console.log("1 appears in the first or last position.");
} else {
    console.log("1 does not appear in the first or last position.");
}
